You can use this command to activate the verification environment:
.\verification_env\Scripts\Activate.ps1

If the user is instructed with running commands, always use the verification environment and always write out the whole command.

If you can find a faster command to run with the same result, please do it.